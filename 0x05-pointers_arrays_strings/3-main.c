#include <stdio.h>

/**
 * main - check the code.
 *
 * Return - Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers, I fear thhe lack of themn - Isaac Asimov";
	_puts(str);
	return (0);
}
